import Navbar from '@/components/Navbar';import Footer from '@/components/Footer';import TicketClient from '@/components/TicketClient';
export default async function Tickets({searchParams}:{searchParams:Promise<{token?:string}>}){const q=await searchParams;return <><Navbar/><main className="ticket-page"><section className="store-hero"><span className="eyebrow">CUSTOMER SUPPORT</span><h1>Ticket yang tidak hilang saat tab ditutup.</h1><p>Simpan kode ticket. Percakapan tersimpan di database hingga admin menutup ticket.</p></section><TicketClient initialToken={q.token||''}/></main><Footer/></>}
