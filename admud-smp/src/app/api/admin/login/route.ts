import {NextResponse} from 'next/server';import bcrypt from 'bcryptjs';import {createAdminToken} from '@/lib/auth';
export async function POST(req:Request){const {username,password}=await req.json();const okUser=username===process.env.ADMIN_USERNAME;const hash=process.env.ADMIN_PASSWORD_HASH||'';const okPass=hash?await bcrypt.compare(password,hash):false;if(!okUser||!okPass)return NextResponse.json({error:'Login gagal.'},{status:401});const r=NextResponse.json({ok:true});r.cookies.set('admud_admin',await createAdminToken(),{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'strict',path:'/',maxAge:28800});return r}
